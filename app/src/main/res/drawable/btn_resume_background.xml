<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Неактивное состояние -->
    <item android:state_enabled="false" android:drawable="@color/color_1"/>

    <!-- Активное состояние (с ripple) -->
    <item android:state_enabled="true" android:drawable="@drawable/btn_resume_ripple"/>
</selector>